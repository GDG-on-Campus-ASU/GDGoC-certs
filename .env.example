# Database Configuration
DB_NAME=gdgoc_certs
DB_USER=postgres
DB_PASSWORD=your_secure_database_password_here

# Frontend URL (used for CORS configuration)
FRONTEND_URL=https://sudo.certs-admin.certs.gdg-oncampus.dev

# CORS Configuration (comma-separated list of allowed origins)
ALLOWED_ORIGINS=https://sudo.certs-admin.certs.gdg-oncampus.dev,https://certs.gdg-oncampus.dev

# API URL for frontend (used during build)
VITE_API_URL=https://api.certs.gdg-oncampus.dev

# Brevo (Sendinblue) SMTP Configuration
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=your_brevo_email@example.com
SMTP_PASSWORD=your_brevo_api_key_here
SMTP_FROM_EMAIL=noreply@gdg-oncampus.dev
SMTP_FROM_NAME=GDGoC Certificate System

# =============================================================================
# IMPORTANT: Authentication via authentik Proxy Provider
# =============================================================================
# This application uses authentik Proxy Provider with Nginx Proxy Manager
# for authentication. Authentication is handled at the reverse proxy level,
# and user information is passed to the application via HTTP headers.
#
# You do NOT need to configure:
# - AUTHENTIK_ISSUER
# - AUTHENTIK_JWKS_URI
# - AUTHENTIK_CLIENT_ID
# - AUTHENTIK_CLIENT_SECRET
# - VITE_AUTHENTIK_URL
# - VITE_AUTHENTIK_CLIENT_ID
# - VITE_AUTHENTIK_RESPONSE_TYPE
# - REQUIRE_ADMIN_GROUP
#
# See docs/AUTHENTIK_SETUP.md for configuring authentik Proxy Provider
# See docs/NGINX_PROXY_MANAGER.md for configuring forward authentication
# =============================================================================

# =============================================================================
# Security: Proxy Authentication Secret (RECOMMENDED for production)
# =============================================================================
# To prevent header spoofing if the application is accidentally exposed directly
# (bypassing the proxy), set a shared secret that the proxy must send in the
# X-Proxy-Auth-Secret header. Generate a strong random secret for production.
#
# Example to generate: openssl rand -hex 32
# PROXY_AUTH_SECRET=your_random_secret_here_min_32_characters
#
# Configure Nginx Proxy Manager to send this header (see NGINX_PROXY_MANAGER.md).
# If not set, the application will log a warning in production but still accept
# requests. This is optional but HIGHLY RECOMMENDED for production deployments.
# =============================================================================
